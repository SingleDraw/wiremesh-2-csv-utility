{
  "name": "mysql",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "ts-node index.ts",
    "dev": "nodemon --watch 'src/**/*.ts' --exec ts-node",
    "build": "tsc",
    "build:dev": "tsc --watch",
    "build:prod": "tsc --outDir dist",
    "build:prod:dev": "tsc --outDir dist --watch",
    "build:prod:dev:clean": "tsc --outDir dist --watch --removeComments",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.5.0",
    "mysql": "^2.18.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/mysql": "^2.15.27",
    "@types/node": "^22.14.1",
    "nodemon": "^3.1.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
